# Change Log

Chronological history of changes. Newest entries at the bottom.

---

- 2026-08-26 — Added `context.md` at project root documenting architecture, routing, server layers, design system, content model, env vars, deployment, conventions, and known gaps.
- 2026-08-26 — Added `changes.md` at project root to track the chronological change history.
- 2026-08-28 — Deleted `server.ts`, `api/index.ts`, `vercel.json` and `VERCEL_DEPLOYMENT.md`; the site is now a fully static build with no backend.
- 2026-08-28 — Deleted `public/_headers` and `public/_redirects` (Cloudflare Pages config, ignored by Azure).
- 2026-08-28 — Added `src/data/siteArchitecture.ts` holding `PRIMARY_PILLAR_KEYWORDS`, `KEYWORD_CLUSTERS`, `GEO_DEFINITION_ANSWER_BLOCKS` and `SITE_ARCHITECTURE`, extracted from the deleted `server.ts`.
- 2026-08-28 — Added `scripts/generate-static-discovery.mts` generating `sitemap.xml`, `robots.txt`, `llms.txt`, `llms-full.txt`, `ai-manifest.json`, `.well-known/*` and eight `/ai/*.json` knowledge files into `dist/` at build time.
- 2026-08-28 — Sitemap grew from 22 hand-maintained URLs to 50 generated from `SITE_ARCHITECTURE`, sub-pages, use cases and discovery files.
- 2026-08-28 — Repointed all `/api/*` knowledge references to `/ai/*.json` across `AiDiscoveryModal`, `DistributorsBacklinkHub`, `authoritativeBacklinks.ts`, `knowledgeGraph.ts` and `ragDiscoveryEngine.ts`.
- 2026-08-28 — Added `src/config/forms.ts` with Web3Forms endpoint, access key and recipient configuration.
- 2026-08-28 — Rewired `BookDemoModal` from `POST /api/book-demo` to a client-side Web3Forms submission, added a honeypot field and an `AlertCircle` error state.
- 2026-08-28 — Fixed `BookDemoModal` reporting success on failed submissions (`submitted` was set in a `finally` block regardless of outcome).
- 2026-08-28 — Added `public/staticwebapp.config.json` with SPA navigation fallback, MIME types, security headers, cache rules and CORS for the AI knowledge files.
- 2026-08-28 — Marked `src/lib/wordpress.ts` and `src/lib/WordPressIntegrationManager.ts` dormant with header comments; they depend on removed proxy routes.
- 2026-08-28 — Fixed type error in `D2cIndustryPage.tsx` where a demo preset lacking `userText` was passed to `setDemoResponse`; `npm run lint` now passes clean.
- 2026-08-28 — Removed `express`, `nodemailer`, `dotenv`, `esbuild`, `@google/genai`, `d3` and related type packages from `package.json`; renamed the package from `react-example` to `silarai-landing-website`; `dev` is now plain `vite` and `build` runs the discovery generator.
- 2026-08-28 — Added `.env.example` (`VITE_WEB3FORMS_ACCESS_KEY`, `SITE_URL`) and typed `import.meta.env` in `src/vite-env.d.ts`.
- 2026-08-28 — Rewrote `context.md` for the static architecture: build-time generation, Web3Forms flow, Azure deployment checklist, re-ranked known gaps.
- 2026-08-28 — Added 301 redirect routes in `staticwebapp.config.json` sending `/blog*`, `/wp-content*`, `/wp-admin*`, `/wp-login.php` and `/feed*` to `blog.silarai.com`.
- 2026-08-28 — Added `DEPLOYMENT.md`: step-by-step runbook for the Azure Static Web Apps deployment and the silarai.com DNS cutover, including relocating WordPress to `blog.silarai.com`.
- 2026-08-29 — Corrected `DEPLOYMENT.md` step 2.2: Hostinger's Add Subdomain creates an empty document root by default, which caused an outage when step 2.3 redirected the live site into it; added the custom-folder requirement, a verification gate, and a recovery procedure.
- 2026-08-29 — Changed `DEPLOYMENT.md` step 2.3 to set `WP_HOME`/`WP_SITEURL` in `wp-config.php` rather than the WordPress admin UI, so the change is reversible when the site stops loading.
- 2026-09-04 — Fixed `.github/workflows/azure-static-web-apps-black-mushroom-050c17800.yml` (the workflow that deploys `landing_website` to `silarai.com`): added an `env:` block injecting `VITE_WEB3FORMS_ACCESS_KEY` (from a GitHub Actions secret) and `SITE_URL` into the `Azure/static-web-apps-deploy@v1` build step. Neither was previously wired in, despite `DEPLOYMENT.md` instructing it — Vite bakes `VITE_*` at build time inside Oryx, so without this the deployed demo-request form fails silently. The `VITE_WEB3FORMS_ACCESS_KEY` GitHub secret must still be added by hand.
- 2026-09-04 — Reverted uncommitted drift in `public/staticwebapp.config.json`: restored the `responseOverrides.404` → `/index.html` (200) rewrite and the `includeSubDomains` directive on the HSTS header, both of which had been locally removed without a corresponding commit or changelog entry.
- 2026-09-04 — Verified the static build end-to-end (fresh `npm install` + `npm run build` off-repo, since the mounted `D:\work\silarai\repo` path rejects some file operations from this environment): compiles clean, sitemap/robots/canonical all resolve to `https://silarai.com`, `blog.silarai.com` appears only in the intended 301 redirect rules. Noted `bun.lock` and `package-lock.json` are both committed — flagged as a candidate cleanup, not changed.
- 2026-09-04 — Verified the live cutover by fetching the production site directly: `https://silarai.com` and `/sitemap.xml` serve correct content, `https://app.silarai.com` serves the separate dashboard product correctly. DNS now runs on Cloudflare (moved off Hostinger's zone editor); the apex `CNAME`/`TXT` records the user configured there are correct (Cloudflare flattens apex CNAMEs; the TXT is Azure's domain-ownership validation).
- 2026-09-04 — Found via live DNS queries: no MX record for `silarai.com` (mail to `info@silarai.com`, where Web3Forms sends demo-request leads, cannot be delivered) and `www.silarai.com` does not resolve (NXDOMAIN). Both documented in `context.md` §11 and `DEPLOYMENT.md`'s status note; neither fixed here — needs a DNS change in Cloudflare, which this session has no access to.
- 2026-09-04 — Retired the WordPress blog (user decision: no longer migrating to `blog.silarai.com`, which was found live but serving an empty page). Removed the `/blog*`, `/wp-content*`, `/wp-admin*`, `/wp-login.php`, `/feed*` redirect routes from `public/staticwebapp.config.json`; marked `DEPLOYMENT.md` Phase 2 and Phase 5 as retired/historical, updated its target-end-state table, post-cutover checklist and rollback section accordingly; updated `context.md` §9 domain layout table and §11 known gaps to match.
- 2026-09-14 — **Merged the SEO and UI work from the `imported_landing_website/` folder (a Google AI Studio export) into `landing_website/`, excluding everything tied to the old pre-Azure deployment.** The import still carries the deleted Express/Vercel architecture (`server.ts`, `api/index.ts`, `vercel.json`, `public/_headers`, `public/_redirects`, hand-written `public/sitemap.xml`/`llms.txt`/`robots.txt`/`.well-known/*`) and a `package.json` with `express`/`nodemailer`/`@google/genai`; none of that was carried over. Verified afterwards that no `/api/*` call, no `@google/genai` import and no `POST /api/book-demo` leaked into `src/` — the only remaining `/api/*` strings are in the already-dormant `src/lib/wordpress*.ts`.
- 2026-09-14 — Added `src/data/seoMetaTable.ts`: master SEO meta table (24 entries) with per-view/sub-page title, meta description, canonical URL, keywords, CTR hook, schema type and rating, plus `findSeoMetaEntry()` and word/char-count compliance helpers.
- 2026-09-14 — Added `src/components/Breadcrumbs.tsx` (BreadcrumbList JSON-LD + visible trail, sibling guide switcher, copy-link button) and wired it into all 12 sub-pages: `AboutPage`, `WhyChoosePage`, `ShopifyComparisonPage`, `WoocommerceComparisonPage`, `AiShoppingAssistantPages`, `AiCommercePlatformPages`, `RetailIndustryPage`, `D2cIndustryPage`, `DistributorsIndustryPage`, `WholesalersIndustryPage`, `ManufacturingIndustryPage`, `FmcgIndustryPage`. Page top padding dropped `pt-24` → `pt-20` where the breadcrumb bar now sits.
- 2026-09-14 — Added `src/components/SectionSeoMetaSnippet.tsx`: render-nothing component that applies title/description/canonical/OG/Twitter tags plus per-view JSON-LD from `seoMetaTable`. Mounted in `App.tsx` inside `<main>`, so its effect runs after `SeoHead` and the meta table is the effective source of truth for title/description/canonical.
- 2026-09-14 — **Fixed the FMCG SEO defect (known gap).** `SeoHead.tsx` gained `case 'fmcg-commerce'` / `case 'fmcg'` with its own title, description, keywords, canonical, SoftwareApplication + Service + FAQPage + BreadcrumbList JSON-LD. The FMCG page had been serving homepage metadata and the bare origin as its canonical URL.
- 2026-09-14 — `SeoHead.tsx` also gained: `case 'contact-us'` and `case 'ai-commerce-marketing-platform'`; the `useSubPageMetaDescription` hook with `SUBPAGE_CTR_VARIANTS` (12 sub-pages × 3 tested variants, all under 60 chars) driving sub-page descriptions and `subpage:*` diagnostic meta tags; and `manufacturingSubPage` added to the effect dependency array, which was missing so manufacturing sub-page changes never re-applied metadata.
- 2026-09-14 — `SeoHead.tsx` `commonOrg` schema updated with real NAP data: `legalName` PSI traders OPC PVT LTD, telephone, `psitraders@outlook.com`, and the Coimbatore postal address (was `info@silarai.com` with no address). Note this contradicts the `info@silarai.com` used elsewhere on the site, and `info@silarai.com` currently has no MX record — worth settling on one contact of record.
- 2026-09-14 — Rewrote `ProductsSection.tsx` from the import: per-card 3-part guide navigation, clickable card/image/title anchors to the dedicated product pages, interactive feature chips with description panels, an `sr-only` semantic backlink network, an `sr-only` comparison matrix table, and ItemList/Table JSON-LD. Gained optional props `onNavigateShopifyComparison`, `onNavigateWoocommerceComparison`, `onSelectD2cPage`, `onSelectManufacturingPage`, all now passed from `App.tsx`.
- 2026-09-14 — Added `src/components/ContactUsPage.tsx` (new `/contact-us` route) — **with the form rewired from the import's `POST /api/book-demo` to Web3Forms** via `src/config/forms.ts`, matching `BookDemoModal`. The import's version set `submitted = true` inside its `catch` block, which is the same false-success bug fixed in `BookDemoModal` on 2026-08-28; the ported version surfaces a real error instead, and adds the honeypot field.
- 2026-09-14 — Rewrote `App.tsx`: all pages, below-the-fold home sections and modals are now `React.lazy` + `Suspense` code-split (addresses the single 1.72 MB chunk gap), modals render only when open, added the `contact-us` view with `/contact-us`, `/contact` and `?page=contact-us` routes in both the `useState` initialiser and `handlePopState`, and mounted `SectionSeoMetaSnippet`.
- 2026-09-14 — `Footer.tsx` gained an `onNavigateContactUs` prop and a Contact Us link in the company column.
- 2026-09-14 — **Deliberately not ported** (user decision): the import's author-facing SEO tooling — `BacklinkAuthorityHub`, `PartnerOutreachSuggester`, `PartnerEmailDrafter`, `SeoMetaTableModal`, `SearchPreviewPane` (~3,100 lines) — which would have shipped internal tools plus dead `/api/*` links into the public bundle. Also not ported: `src/server/seoMetaTableBackend.ts` (no runtime that consumes it), the import's `AiDiscoveryModal`/`DistributorsBacklinkHub`/`Navbar` edits that reintroduce `/api/*` URLs, and its `BookDemoModal`.
- 2026-09-14 — **Still outstanding from the import:** `HeroSection.tsx` (adds two image tabs + a lightbox, and needs `shopping_assistant_ui_1788795889801.jpg` and `commerce_cloud_platform_1788795904634.jpg` copied into `src/assets/images/`) and `AiCommerceMarketingPlatformPage.tsx` (a new ~1,000-line pillar page for the "AI Commerce & Marketing Platform" keyword cluster; `SeoHead` already has its metadata case, but no route is wired). Neither is blocking a build.
- 2026-09-14 — **Enquiry forms switched to a `mailto:` handoff; Web3Forms removed entirely** (user decision). `BookDemoModal` and `ContactUsPage` now build a pre-filled message with `buildMailtoUrl()` and open the visitor's own mail client; the visitor presses Send. Confirmation copy changed from "Demo Request Dispatched!" / "Thank You!" to "One last step — press Send", since at that point nothing has been sent. Both screens show the destination address as copyable text with a Copy button plus the phone number, because visitors with no registered mail client would otherwise see nothing happen.
- 2026-09-14 — **Did not port the import's updated `BookDemoModal`/`ContactUsPage`.** Its "reduced backend dependency" rewrite wrote submissions to `localStorage` on the visitor's own device, waited 350 ms, then displayed "An automated notification email with your request details has been routed directly to psitraders@outlook.com" — no email is sent and no one at SilarAI can read the visitor's localStorage, so every lead would have been lost behind a false confirmation. Same class of defect as the 2026-08-28 `finally`-block fix. The delivery mechanism was replaced rather than the message alone.
- 2026-09-14 — Rewrote `src/config/forms.ts` as the single source of truth for contact details (`CONTACT_EMAIL` = `psitraders@outlook.com`, `CONTACT_PHONE`, `CONTACT_LEGAL_NAME`, `CONTACT_ADDRESS`) plus `buildMailtoUrl()` (trims trailing body lines to stay under ~1800 chars, since some mail clients silently truncate long `mailto:` URLs), `openMailClient()` and `copyToClipboard()`. `ContactUsPage`'s hardcoded contact block now reads from these constants.
- 2026-09-14 — Removed the now-unused `VITE_WEB3FORMS_ACCESS_KEY` from the deploy workflow's `env:` block, from `src/vite-env.d.ts` and from `.env.example`; the GitHub Actions secret is no longer needed and can be deleted. `SITE_URL` injection is unchanged. Updated `DEPLOYMENT.md` Phase 3 and Phase 6 and `context.md` §4.7 / §8 / §9 / §11 to describe the handoff, including its trade-offs: no server-side record of enquiries, and no way to measure forms composed but never sent.
- 2026-09-15 — Investigated the reported `Refused to apply style from '/assets/index-CQquVHon.css' … MIME type ('text/html')` error against the live site: that file returns `200 text/css` and the page loads without console errors, so it is not currently occurring. Reproduced the mechanism instead — `responseOverrides.404` in `staticwebapp.config.json` turns **any** missing file under `/assets/` into a `200` serving `index.html` (verified: `/assets/index-DOESNOTEXIST.css` → `200 text/html`), so a stale `index.html` requesting a superseded Vite content hash produces exactly that error rather than an honest 404. The override response carries `max-age=30` (not the `/assets/*` `immutable` year), so it self-heals on reload. Noted that the pending un-deployed changes will alter both the CSS and JS hashes, so a brief window of this is expected on the next deploy. No code changed.
- 2026-09-16 — **Site-wide rebrand ported from the import: `src/index.css` swapped to the official brand palette.** Dark Teal `#245668` (structural), Teal Green `#0D8F81` (secondary), Persimmon `#F47A38` (CTA), Off-White `#F8F9FA` (canvas), replacing plum `#584053` / soft teal `#8DC6BF` / apricot `#FCB666` / coral `#F97B4F`. The import deliberately **remaps the legacy token names** onto the new ramps — `plum-*` is now Dark Teal, `peach-*`/`coral-*` are both Persimmon — so every existing component recolours with no component edits. Body text/canvas moved to `#183a47` on `#F8F9FA`; grid, radial-glow, shadow and scrollbar utilities retuned to the new hues. `context.md` §5 rewritten. **Not updated:** `index.html` still carries `theme-color: #2b0f38` and a purple favicon — both legacy plum, now clashing.
- 2026-09-16 — Added `src/data/internalLinkingClusters.ts` + `src/components/InternalLinkingSection.tsx`: an `sr-only` topical internal-link block emitting `ItemList`/`SiteNavigationElement` JSON-LD per view. **Corrected the import's `path` values while porting** — it used `/d2c-brands`, `/manufacturing`, `/retail-commerce`, `/distributors`, `/wholesalers`, none of which the router resolves. Because the SWA fallback returns `index.html` with a 200, those would have been soft 404s serving homepage content under distinct URLs; they now point at the real `/industries/*` routes. Also switched the JSON-LD origin from `https://www.silarai.com` (NXDOMAIN) to the canonical apex.
- 2026-09-16 — `getInternalLinkingForPage()` rewritten to be lookup-safe: the import's version returned `undefined` for any alias whose cluster was absent, which would throw when the component read `cluster.links`. It now falls back to the `home` cluster in every branch. **Only 3 of the import's 16 clusters are ported so far** (`home`, `about`, `why-choose-us`); the remaining 13 page types fall back to the home cluster — valid output, just not page-specific.
- 2026-09-16 — Added `src/components/AllInOneSection.tsx` and `src/components/CustomDomainSection.tsx`, both lazy-loaded into the home page below `WhySilarAi`. Fixed a latent bug in the import's `AllInOneSection` (it destructured `FeatIcon` then rendered a hardcoded `CheckCircle2`, leaving an unused binding); dropped unused `accent` fields and unused lucide imports from both; and replaced a hardcoded legacy-purple `#2d1145` gradient stop in `CustomDomainSection` with `#183a47` to match the new palette.
- 2026-09-16 — `App.tsx`: wired the three new sections and added `handleNavigateToView(view, options)`, which maps an internal-link cluster's `view` id onto the existing navigation handlers.
- 2026-09-16 — Reviewed but did not port: the import's `SeoHead` (−4 lines vs ours, identical 15-case set — the delta is case ordering only) and `WordPressIntegrationManager` (−32 lines removing its `/api/wordpress/*` proxy calls). The latter is the "reduced backend dependency" change, but that module is dormant — nothing imports it — so porting it would only shuffle dead code. `ContactUsPage` and `BookDemoModal` were unchanged in this import round and keep the 2026-09-14 `mailto:` handoff.
- 2026-09-15 — Added `imported_landing_website/` to the root `.gitignore` — it is a Google AI Studio export kept only as a merge reference and must never be committed or built (it still carries `server.ts`, `api/`, `vercel.json` and an express/nodemailer/@google/genai `package.json`). `context.md` §4.0 updated to say so.
